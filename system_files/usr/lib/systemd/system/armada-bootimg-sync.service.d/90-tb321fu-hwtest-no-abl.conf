# Preserve automatic /KERNEL regeneration, but remove the base unit's second
# ExecStop that invokes armada-abl-finalize. This image must never update ABL.
[Service]
ExecStop=
ExecStop=/usr/libexec/armada/armada-bootimg-update
